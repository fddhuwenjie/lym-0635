# 会议室设备联动与故障报修系统

## 一、项目用途

本系统用于企业内部会议室设备的全生命周期管理，解决以下核心问题：

1. **设备管理混乱**：统一管理投影、音响、门禁、网络四类设备及会议室
2. **会议调度冲突**：通过时间片校验避免同一设备被多个会议占用
3. **故障报修滞后**：支持会议前检查、会议中使用环节直接上报故障
4. **状态可视性差**：提供设备状态墙、会议日历实时展示运行状态
5. **数据统计难**：支持设备占用、检查结果、故障处理等报告导出

系统支持两类用户角色：
- **系统管理员**：维护会议室和设备、处理故障、查看所有数据、导出报告
- **普通用户**：创建会议、预约设备、上报异常

---

## 二、启动方式

### 环境要求
- Node.js >= 18
- npm 或 pnpm

### 安装与运行

```bash
# 1. 进入项目目录
cd lym-0635

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev

# 4. 浏览器打开
# http://localhost:5173/
```

### 其他命令

```bash
# 类型检查
npm run check

# 生产环境构建
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

---

## 三、技术栈

- **前端框架**：React 18 + TypeScript
- **构建工具**：Vite 6
- **UI样式**：Tailwind CSS 3
- **状态管理**：Zustand 5（含 localStorage 持久化）
- **路由**：React Router DOM 7
- **图标**：Lucide React
- **日期处理**：date-fns
- **数据持久化**：浏览器 localStorage

---

## 四、功能模块

| 模块 | 路由路径 | 核心功能 |
|------|---------|---------|
| 设备状态墙 | `/` | 会议室状态概览、设备实时状态、今日会议统计、会议室详情弹窗 |
| 会议日历 | `/calendar` | 月视图日历、会议详情查看、临时换房（设备清单同步） |
| 会议管理 | `/meetings` | 会议列表、开始/取消/结束会议、使用反馈、异常上报 |
| 创建会议 | `/meetings/create` | 选择会议室与时间、联动设备勾选、设备可用性校验 |
| 设备管理 | `/devices` | 会议室增删改查、投影/音响/门禁/网络设备增删改查 |
| 检查任务 | `/inspections` | 检查任务列表、逐项设备检查、异常上报、完成检查 |
| 故障报修 | `/faults` | 故障单列表、分配处理人、标记修复、关闭工单、处理进度时间线 |
| 报告导出 | `/reports` | 四种报告类型导出、CSV格式、导出历史记录 |

---

## 五、验收路径

### 5.1 基础功能验收

1. **启动系统**
   - 访问首页，确认左侧7个菜单项正常显示
   - 确认设备状态墙展示4个会议室卡片及设备统计数据

2. **设备管理验收**
   - 进入「设备管理」→ 切换到「会议室管理」Tab
   - 尝试添加/编辑/删除会议室，确认操作正常
   - 切换到「设备管理」Tab，确认4类设备（投影/音响/门禁/网络）均有数据
   - 尝试修改某设备状态为「故障」，确认设备列表状态标签变化

### 5.2 会议创建与设备联动验收

3. **创建会议**
   - 进入「会议管理」→ 点击「创建会议」
   - 填写会议主题、选择会议室、设置时间
   - 在联动设备区域，确认设备显示「可用/故障/已占用」状态
   - 选择全部可用设备，点击「创建会议」，提示成功

4. **设备故障拦截验收**
   - 进入「设备管理」，将某设备状态改为「故障」
   - 再次创建会议，勾选同一时间段和会议室
   - 确认故障设备显示红色「故障中」标签且不可勾选

5. **设备占用冲突验收**
   - 创建第一个会议，选择会议室A、时间段T、勾选全部设备
   - 创建第二个会议，选择同一会议室A、同一时间段T
   - 确认所有设备均显示「已占用」且不可勾选

### 5.3 检查任务与会议开始验收

6. **检查任务生成验收**
   - 进入「检查任务」页面
   - 确认新建会议对应生成了待检查任务，检查项为勾选的设备清单

7. **检查未完成拦截验收**
   - 进入「会议管理」
   - 找到待开始的会议，点击「开始」按钮
   - 确认提示「设备检查未完成，不可开始会议」

8. **执行设备检查**
   - 进入「检查任务」→ 点击「执行检查」
   - 将所有设备标记为「正常」→ 点击「完成检查」
   - 返回会议列表，确认「开始」按钮变为可用

9. **异常上报验收**
   - 另一个检查任务执行时，将某设备标记为「异常」
   - 填写异常描述，点击「上报故障」
   - 进入「故障报修」页面，确认自动生成了对应故障单
   - 检查该设备状态变为「故障」

### 5.4 会议结束与反馈验收

10. **开始并结束会议**
    - 在会议列表点击「开始」启动已检查完成的会议
    - 状态变为「进行中」后，点击「结束」
    - 填写使用反馈，可选勾选异常设备并填写异常说明
    - 确认会议结束后异常设备自动生成故障工单

### 5.5 故障处理与设备恢复验收

11. **故障处理流程**
    - 进入「故障报修」→ 找到待处理故障单
    - 点击「分配处理人」→ 填写处理人姓名
    - 故障状态变为「处理中」
    - 点击「标记修复」→ 填写修复说明
    - 故障状态变为「已修复」
    - 点击「关闭工单」→ 故障状态变为「已关闭」

12. **故障修复后重新可选验收**
    - 进入「设备管理」确认该设备状态恢复为「正常」
    - 创建新会议，确认该设备可正常勾选
    - 验证故障单未关闭拦截：处理到「已修复」但未「关闭工单」时，设备仍显示故障

### 5.6 特殊场景验收

13. **会议取消释放设备验收**
    - 创建一个会议并勾选设备
    - 进入会议列表点击「取消」
    - 立即创建另一相同时间段的会议
    - 确认设备已释放、可重新勾选

14. **临时换房设备清单同步验收**
    - 进入「会议日历」→ 点击某个会议查看详情
    - 点击「临时换房」→ 选择不同会议室
    - 确认换房后：
      - 会议关联的会议室ID更新
      - 设备清单自动替换为新会议室对应类型的可用设备
      - 检查任务的检查项同步更新为新设备清单

15. **报告导出验收**
    - 进入「报告导出」→ 选择「综合报告」
    - 设置时间范围，点击「生成并导出报告」
    - 确认下载的CSV文件包含：
      - 设备占用记录（会议、设备、时间）
      - 检查结果（检查项、通过/异常数）
      - 故障处理人及处理进度
      - 报告生成时间

16. **数据持久化验收**
    - 创建一条会议或故障记录
    - 刷新浏览器页面（F5）
    - 确认所有数据（会议记录、设备占用、故障处理、导出历史）均保持一致
    - 关闭浏览器重新打开，数据仍然存在

---

## 六、关键数据流转

### 6.1 核心实体关系

```
会议室 (Room) 1──N 设备 (Device)
会议 (Meeting) N──N 设备 (Device) [通过 deviceIds 关联]
会议 (Meeting) 1──1 检查任务 (InspectionTask)
设备 (Device) 1──N 故障单 (FaultTicket)
```

### 6.2 会议创建数据流

```
用户填写表单
    ↓
校验会议室时间冲突（isRoomAvailable）
    ↓
逐台校验设备：
  ├─ 设备是否故障（isDeviceFaulty → 设备status + 是否有未关闭故障单）
  └─ 设备是否被占用（isDeviceAvailable → 时间区间重叠检查）
    ↓
创建 Meeting 记录（status = scheduled）
    ↓
自动生成 InspectionTask 检查任务
    ↓
关联 CheckItem 检查项（基于勾选的设备清单）
```

### 6.3 会议开始数据流

```
用户点击「开始会议」
    ↓
检查会议状态必须为 scheduled
    ↓
查找关联的 InspectionTask
    ↓
校验检查任务状态必须为 completed
    ↓
校验所有检查项 checked=true 且 normal=true
    ↓
更新 Meeting.status = in_progress
```

### 6.4 故障上报与恢复数据流

```
【故障上报】
  检查异常 / 会议结束异常 / 手动新建故障单
      ↓
  创建 FaultTicket（status = open）
      ↓
  更新 Device.status = fault

【故障处理】
  分配处理人 → FaultTicket.status = processing + handler
      ↓
  标记修复 → FaultTicket.status = fixed + fixNote + fixTime
      ↓
  关闭工单 → FaultTicket.status = closed + closeTime
      ↓
  检查设备是否还有其他未关闭故障单
      ├─ 无 → Device.status = normal
      └─ 有 → Device.status 保持 fault
```

### 6.5 临时换房数据流

```
用户选择新会议室
    ↓
校验新会议室在原时间段是否可用
    ↓
获取原会议勾选的设备类型列表
    ↓
在新会议室中匹配同类型且可用的设备
    ↓
更新 Meeting.roomId 和 Meeting.deviceIds
    ↓
同步更新 InspectionTask：
  ├─ deviceIds 替换为新设备
  ├─ checkItems 重建（基于新设备）
  └─ status 重置为 pending
```

### 6.6 会议取消释放数据流

```
用户取消会议（仅 scheduled 状态可取消）
    ↓
Meeting.status = cancelled
    ↓
设备占用释放：
  由于 isDeviceAvailable 查询时自动过滤 cancelled 状态会议，
  无需额外操作，设备即刻可被其他会议勾选
```

### 6.7 持久化存储

所有数据通过 Zustand 的 persist 中间件自动同步至浏览器 localStorage，键名为 `meeting-room-system-storage`。包含以下数据：
- `rooms`：会议室列表
- `devices`：设备列表
- `meetings`：会议列表
- `inspections`：检查任务列表
- `faults`：故障单列表
- `exportRecords`：导出历史记录

---

## 七、项目目录结构

```
lym-0635/
├── .trae/documents/           # PRD及技术架构文档
├── src/
│   ├── components/            # 通用UI组件
│   │   ├── Badge.tsx          # 状态标签
│   │   ├── Button.tsx         # 按钮
│   │   ├── Layout.tsx         # 整体布局（含侧边栏）
│   │   ├── Modal.tsx          # 弹窗
│   │   ├── PageHeader.tsx     # 页面标题
│   │   └── Sidebar.tsx        # 侧边导航
│   ├── data/
│   │   └── initialData.ts     # 初始模拟数据
│   ├── pages/                 # 页面组件
│   │   ├── Calendar.tsx       # 会议日历
│   │   ├── CreateMeeting.tsx  # 创建会议
│   │   ├── Dashboard.tsx      # 设备状态墙
│   │   ├── Devices.tsx        # 设备管理
│   │   ├── Faults.tsx         # 故障报修
│   │   ├── Inspections.tsx    # 检查任务
│   │   ├── Meetings.tsx       # 会议管理
│   │   └── Reports.tsx        # 报告导出
│   ├── store/
│   │   └── appStore.ts        # Zustand全局状态（含全部业务逻辑）
│   ├── types/
│   │   └── index.ts           # TypeScript类型定义
│   ├── App.tsx                # 路由配置
│   ├── index.css              # 全局样式
│   └── main.tsx               # 入口文件
├── index.html
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```

---

## 八、关键拦截规则总结

| 拦截场景 | 触发位置 | 拦截条件 | 提示信息 |
|---------|---------|---------|---------|
| 设备故障 | 创建会议勾选设备 | 设备status=fault 或 存在未关闭故障单 | 设备显示「故障中」不可选 |
| 设备占用冲突 | 创建会议保存 | 同时间段设备已被其他会议占用 | 提示设备被占用 |
| 检查未完成 | 点击开始会议 | 检查任务status≠completed | 「设备检查未完成，不可开始会议」 |
| 故障单未关闭 | 创建会议勾选设备 | 设备存在status≠closed的故障单 | 设备显示「故障中」不可选 |
| 进行中会议取消 | 点击取消会议 | Meeting.status=in_progress | 「进行中的会议不可取消」 |
| 临时换房冲突 | 确认换房 | 新会议室时间段被占用 | 「新会议室在该时间段不可用」 |
